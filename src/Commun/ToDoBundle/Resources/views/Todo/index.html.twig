{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Todo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Added</th>
                <th>Modified</th>
                <th>Value</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_todo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.added %}{{ entity.added|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.modified %}{{ entity.modified|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.value }}</td>
                <td>{{ entity.status }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_todo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_todo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_todo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
