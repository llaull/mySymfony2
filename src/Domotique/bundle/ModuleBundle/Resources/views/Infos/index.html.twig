{% extends "CommunUserBundle:Admin:MainWrapper.html.twig" %}
{% block title %}{% endblock title %}
{% block h1_header %}{% endblock h1_header %}
{% block h1_header_s %}-{% endblock h1_header_s %}

{% block dynamic_body %}

    <div class="col-md-12 bootstrap-grid">

        <div class="powerwidget" id="datatable-basic-init" data-widget-editbutton="false">
            <header>
                <h2>tableaux</h2>
                {#<h2>plup#}
                {#<small>blbl</small>#}
                {#</h2>#}
            </header>
            <div class="inner-spacer">

                <ul class="btn-group">
                    <li>
                        <a class="btn btn-success btn-sm" role="button"
                           href="{{ path('admin_domotique_module_emplacement_new') }}">New</a>
                    </li>
                </ul>
                <table class="table table-striped table-hover" id="table-1">
                    <thead>
                    <tr>
                        <th>Moduleref</th>
                        <th>Modulenom</th>
                        <th>Nrfid</th>
                        <th>Moduletype</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for entity in pagination %}
                        <tr>
                            <td>{{ entity.moduleRef }}</td>
                            <td>{{ entity.moduleNom }}</td>
                            <td>{{ entity.nrfId }}</td>
                            <td>{{ entity.moduleType }}</td>
                            <td>
                                <div class="btn-group">
                                    <a class="btn btn-warning btn-sm" role="button"
                                       href="{{ path('admin_domotique_module_infos_edit', { 'id': entity.id }) }}">edit</a>
                                    <a class="btn btn-danger btn-sm confirm-delete" role="button"
                                       data-href="{{ path('admin_domotique_module_infos_delete', { 'id': entity.id }) }}"
                                       href="{{ path('admin_domotique_module_infos_delete', { 'id': entity.id }) }}">Delete</a>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
                <div class="navigation">
                    {{ knp_pagination_render(pagination) }}
                </div>
            </div>
        </div>
    </div>

{% endblock dynamic_body %}
