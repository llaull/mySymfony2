{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Log list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Sonsorvalue</th>
                <th>Sensorid</th>
                <th>Created</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_sensor_log_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.sonsorValue }}</td>
                <td>{{ entity.sensorId }}</td>
                <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_sensor_log_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_sensor_log_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_sensor_log_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
