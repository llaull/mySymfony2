{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlogComment list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created</th>
                <th>Authorname</th>
                <th>Authormail</th>
                <th>Contenu</th>
                <th>Publied</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_carnet_blog_comment_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.authorName }}</td>
                <td>{{ entity.authorMail }}</td>
                <td>{{ entity.contenu }}</td>
                <td>{{ entity.publied }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_carnet_blog_comment_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_carnet_blog_comment_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_carnet_blog_comment_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
